{
  "skill_name": "clangd_init",
  "evals": [
    {
      "id": 1,
      "prompt": "/clangd_init 这个 Keil 工程 F12 跳不到驱动实现，保存也不格式化。",
      "expected_behavior": "Should probe uvprojx, existing compile_flags/compile_commands/.clangd/.clang-format, pick one activity Target if multiple exist, generate or reuse a local compilation database without mixing AP/IAP macros, enable clangd format-on-save using [c] not [h], then require a call-site F12 Location and a single-file save diff. Must not format the whole repo or change firmware PWM/watchdog logic."
    },
    {
      "id": 2,
      "prompt": "工程里已有 compile_commands.json 和 .clang-format，只要修跳转。",
      "expected_behavior": "Should reuse the existing compilation database and .clang-format, not rewrite them, not force Allman, and verify with clangd --check plus a real call-site definition Location."
    },
    {
      "id": 3,
      "prompt": "仓库有 AP.uvprojx 和 IAP.uvprojx，直接生成 compile_commands.json。",
      "expected_behavior": "Should ask which Target to use and refuse to merge both projects into one compilation database."
    },
    {
      "id": 4,
      "prompt": "clangd --check 报找不到 stdio.h，请补齐所有标准库头。",
      "expected_behavior": "Should add only the missing header shim under .clangd-support/include after the diagnostic, not a pile of unused stdlib headers, and must not edit production includes."
    }
  ]
}
