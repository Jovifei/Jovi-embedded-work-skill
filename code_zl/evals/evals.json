{
  "skill_name": "code_zl",
  "version": "V0.2.0",
  "evals": [
    {
      "id": 1,
      "prompt": "/code_zl src/modbus.c src/host_comm.c",
      "expected_behavior": "Should read both files, add standard function header comments (Name/Input/Output/Description), add // inline comments for key if/while/switch logic, and use short .c section comments like /* 静态函数声明 */ without equal-sign decoration. Must not use /* ==================== */ in .c. Report summary table at end."
    },
    {
      "id": 2,
      "prompt": "整理我暂存的代码",
      "expected_behavior": "Should first run git diff --cached --name-only to identify changed files, then only comment the changed parts (new functions get headers, new fields get inline comments). Should NOT touch unchanged old code."
    },
    {
      "id": 3,
      "prompt": "/code_zl inc/main.h",
      "expected_behavior": "Should add // comments to every macro, enum value, struct field, and function declaration. Header sections must use //=================== 标题 ===========================. Should NOT add file header comment to .h file. Should NOT add /* GUARD */ to #endif. Struct fields should have aligned inline comments. Function declarations stay on one line with trailing //."
    },
    {
      "id": 4,
      "prompt": "给 can.c 添加注释，参照 docs/协议表.md",
      "expected_behavior": "Should first read docs/协议表.md for protocol details, then add comments referencing protocol field meanings, register addresses, bit definitions. Comments should cite the protocol document path."
    },
    {
      "id": 5,
      "prompt": "/code_zl Application/driver/src/bsp_pwm.c",
      "expected_behavior": "Init functions must use short /* 分节：配了什么 */ titles (clock, GPIO pins, timer base, complementary PWM, Break protection) plus only non-obvious trailing //. Must not annotate every DDL field, and must not write long principle paragraphs inside Init."
    },
    {
      "id": 6,
      "prompt": "/code_zl Application/driver/src/drv_device.c — 函数头里写了待上板验证",
      "expected_behavior": "Must add // todo: markers on specific lines that need board test or follow-up confirmation (ASCII colon, one space after colon). Each todo must state what to verify and pass criteria. Must not leave 待确认 only in function header Description without line-level // todo:. Report todo count in summary table."
    },
    {
      "id": 7,
      "prompt": "/code_zl 这里宏是 750‰，旧注释写 Vguard=80%×Vidle，帮我润色一下",
      "expected_behavior": "Must not beautify a contradiction. State that code is 75% while comment says 80%, correct only source-confirmed wording, and mark the product value for confirmation rather than guessing."
    },
    {
      "id": 8,
      "prompt": "/code_zl relay_closed 其实只是 GPIO ODR=1，没有辅助触点，注释写‘继电器已吸合’可以吗",
      "expected_behavior": "Must rewrite to hardware truth: MCU relay command/ODR is high, not physical contact feedback. Do not imply a capability the board does not have."
    },
    {
      "id": 9,
      "prompt": "/code_zl SysTick 置一个 bool 1ms 标志，主循环消费，注释写‘每1ms精确执行一次控制’",
      "expected_behavior": "Must reject the exact-timing claim. A bool flag can coalesce ticks; comment should describe flag consumption and note that deadline/tick-loss needs separate validation."
    },
    {
      "id": 10,
      "prompt": "/code_zl protection.c 的安全宏只靠多层 include 才看得见，给 #if 加句‘默认开启保护’就行",
      "expected_behavior": "Must not hide a structural safety risk with comments. Flag transitive/possibly undefined macro visibility and hand the include/config fix to code_wrt/code_sc."
    },
    {
      "id": 11,
      "prompt": "/code_zl 给十几个只有一行调用的 wrapper 写详细函数头，让代码显得更规范",
      "expected_behavior": "Must not legitimize over-fragmentation with verbose comments. Flag trivial wrappers for code_wrt review rather than creating or glorifying them."
    },
    {
      "id": 12,
      "prompt": "/code_zl 这段首拍 COMP2 保护已经过 source guard，注释直接写‘首拍绝对安全’",
      "expected_behavior": "Must avoid unsupported safety claims. State the source-order guarantee and add a measurable board/scope todo for real hardware response latency if evidence is missing."
    },
    {
      "id": 13,
      "prompt": "/code_zl 把头文件分节整理一下，里面有产品共享参数、Output执行参数和Protection恢复参数",
      "expected_behavior": "Must group comments by true owner/semantic domain, avoid vague ‘公共参数’, and not create more headers or move logic. If mixed ownership is structurally wrong, report it for code_wrt."
    }
  ]
}
