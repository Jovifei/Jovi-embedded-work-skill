{
  "skill_name": "codex-memory",
  "cases": [
    {
      "prompt": "在 D:\\work\\smart-controller-gd32f4\u0027s bootloader 目录排查启动问题，先读取项目记忆，不要创建任何笔记。",
      "expect": ["run configuration and longest-path project discovery", "resolve bootloader to the parent memory_id", "load bounded current knowledge slots", "report NO_PROJECT_MEMORY without writes when absent"]
    },
    {
      "prompt": "查找当前项目以前关于 Modbus 超时和启动晶振的经验，再结合源码判断。",
      "expect": ["run bounded targeted search", "return relative paths and line numbers", "treat Vault as prior context rather than proof", "redact sensitive content"]
    },
    {
      "prompt": "把当前项目的已验证 Bug 根因、理论解释和最终修复归档到 Obsidian，先做 DryRun。",
      "expect": ["create only overview/relations/progress/decisions/workflow operations", "reject plan slot", "use live managed blocks", "return MEMORY_UPDATED only after Apply or MEMORY_SYNC_BLOCKED on conflict"]
    },
    {
      "prompt": "同步工程 docs 到 Obsidian。",
      "expect": ["copy original Markdown bytes", "exclude 00 reading/template files, sync manifests, superpowers plans/specs, and tasks", "store mirror state outside Vault", "keep Boot documents below the parent bootloader path"]
    },
    {
      "prompt": "在公司电脑同步到我家里的 Obsidian。",
      "expect": ["resolve company profile", "block personal synchronization without an approved policy and root", "do not copy staging"]
    }
  ]
}
