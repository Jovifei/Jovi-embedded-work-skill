interface:
  display_name: "Codex Memory"
  short_description: "Load, search, checkpoint, and mirror mapped project memory."
  default_prompt: "Use $codex-memory to load/search mapped Obsidian project memory and checkpoint verified durable results with filtered Markdown docs."
